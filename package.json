{
  "name": "reprise-media-repair",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "GPL-3.0-or-later",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "dev": "python3 -m http.server 4173 --bind 127.0.0.1 --directory dist"
  }
}
